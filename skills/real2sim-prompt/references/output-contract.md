# 输出与评测记录约定

沿用现有兼容目录，不为接入技能迁移数据。新增或调整记录时才读取本文件。

## 最小可交付记录

- 场景和资产版本、坐标单位、相机身份、真实状态/估计/插值/模拟的来源。
- 源帧、各视角源索引/时间戳、Blender帧、视频帧映射；硬件同步未知则明确未知。
- 工程文件、第二步第 8 环节所需视角 Real RGB / Blender RGB / MuJoCo RGB 三方图像、交互事件和逐关键帧五问；视频按用户请求导出。
- 一份交付清单指向场景、配置、评测及输出哈希。深度、实例分割等仅在任务要求时生成。

## 质量与接受范围

`acceptance_profile`采用`usable_reconstruction / precise_alignment / high_fidelity`。

分别记录：`technical_observation`（观测或数值）、`status`（当前用途下的判断）、`blocking`、`acceptance_basis`（用户接受或证据）、`known_deviations`。用户接受时保存原话/范围和场景哈希，保留历史评测，不篡改原始误差。

状态：`pass / pass_with_notes / fail / review`。最大差异描述可为`observed`；未检查项写`not_evaluated`，不能伪装为技术通过。任务相关的阻塞项计入失败分母；观察项、已接受偏差和遮挡未知分别统计，不将其全部加成“失败率”。

用户认可当前重建画面可使其在当前用途下通过，不代表未知接触力、精确三维或严格时序已认证。项目的一次接受不自动构成后续项目的通用容差。

## 事件与五问

事件与覆盖字段按[事件与复核规范](events-and-review.md)记录：真实/模拟事件分开，包含前中后角色、对象关系、源索引、匹配/未匹配与不确定区间。起止或仓库参考图标为 context，不计作事件覆盖。汇总实际观察帧×视角数与物理扫描步数，保留未检查项；不能用计划数量作实际分母。

`fresh_start` 另存输入允许清单与来源边界。旧结果、清理清单和身份对应表放在输入范围外，事件表从新观察生成；报告说明是否真正由独立会话执行，不能把文件隔离写成已清空对话。

每个关键帧×视角一条记录，五问均保留：

`largest_difference / camera_alignment / relative_object_alignment / penetration_and_contact / reconstruction_fidelity`

每问含状态、一句针对当前帧的观察、证据和是否阻塞。相机/几何共享证据引用文件或记录ID，避免重复长段解释。局部更新可用`inherited_from`引用依赖仍有效的既有结论；保存当前修改项与受影响问题，报告生成器展开完整表格。未看过的帧不能用邻帧结论代填。

## 几何与区间

全场景首次审计记录对象/碰撞代理、结构、支撑及排除对。增量审计记录检查范围、被修改的依赖、复用的基线及理由。

问题区间记录异常首末帧、前后稳定边界、修正内容、输出帧与子帧步长、实际样本数、最大深度/间隙、失败与待复核样本。无连续检测时不宣称连续保证。严格零相交和容差内通过分开表述。

## 导出核验与报告

视频记录工程/渲染哈希、相机顺序、源帧映射、尺寸、帧数、播放时间及编码。完整解码，抽查首末帧、修复区间和关键事件与源图对应。

最终摘要只展示本轮改动、当前验收等级、已通过范围、阻塞问题、已接受偏差及产物链接。完整数据留文件；不要把历史严格评测的fail未经解释当作用户认可版本的当前结论，也不要删除历史记录。

## 第二步：MuJoCo 物理执行记录

仅本步需要新增这些记录；允许现有文件承载，不强制每个字段独占文件。

- **输入与复现**：源 `.blend`、MJCF、显示/碰撞资产、机器人描述、初始 keyframe、控制轨迹/策略、物理参数及版本哈希。共享资产路径可解析；磁盘最终模型可独立初始化运行。
- **参数来源与差异**：质量/惯量/摩擦/驱动的 `measured / asset_nominal / estimated / calibrated` 来源；校准前后值、单位、修改理由、保留的实测记录及辅助约束（如有）。仿真调优值不能改标成实测。
- **分层状态**：`model_import / kinematic_replay / contact_tests / native_task` 分别记录 `pass / pass_with_notes / fail / not_evaluated`。保存 `execution_mode`（如状态回放、驱动器物理执行或辅助执行），禁止汇总成无法区分的单一“成功”。
- **物理事件与关键帧**：实际物理时间、源索引、对准偏差、闭合目标/实际值、各侧接触对及法向力、双侧承载定义与时长/占比、相对工具滑移、释放及最终支撑。五问只更新被物理运动影响的项，共享接触证据可引用。
- **完整回归**：真实物理步长、步数/时长、检查区间、最大穿入、力峰及中位数、驱动力矩峰值、求解器警告、物体整体到达目标与最终速度；标明坐标一致性误差是在比较 Blender 还是实测数据。
- **扰动与验收**：标称结果、每个扰动配置和结果、真实分母、既定容差、未解决项。初触过渡、短暂力波动、状态缺测不能被改成严格零误差或百分之百稳定。
- **视频与入口**：注明渲染来自哪次原生状态序列，区分回放、实际执行和失败诊断视频；源范围与额外稳定时段分开记录。最终清单指向当前模型、控制、报告与视频，旧失败证据保留并标注版本。

### 第二步状态与性能摘要

任务结果与几何/数值状态分别记录；总体状态综合本轮必需检查。成功返回值和退出码不能覆盖超容差项。支撑力按物体—支撑关系聚合，两侧夹爪力分别汇总；保留接触点数与汇总方式以避免阈值误判。

性能记录包含相同模型/配置标识、阶段、实际步数、物理时间、墙钟耗时及代表性接触规模。非隔离的前后运行不声称单变量因果。候选提前结束记录原因、已覆盖范围及可恢复状态位置，不把未执行的剩余阶段标通过。

## 标定、显示与精细渲染记录

允许现有清单承载，不强制新增多份文件：

- **对齐证据**：拟合/留出观测ID、视角、分辨率、观测点数、重投影误差、先验和已知畸变/不可观测项。共同拟合与独立验证分开。
- **外观完成度**：主要对象材质类别、纹理来源/尺度、背景补全项、灯光与色彩管理、已接受残差；区分 `diagnostic_preview / blender_reconstruction / blender_native_physics` 图像用途。
- **回传绑定**：显示对象与物理 body/link 的映射、局部偏置、轨迹来源/哈希、采样/插值、各视角时间映射、求值变换误差及重开工程复核结果。
- **可复现渲染**：显示工程、外部资产、相机、引擎/版本/设备、样本/降噪、输出尺寸/像素比例、图像与原生轨迹哈希；记录已检查的样帧与未检查项。缺失资源不得静默回退后仍标为精细渲染通过。
- **试验假设**：校准目的、改变变量、预期/实测响应、实际生效参数和接受/拒绝理由；敏感性试验不写成已完成物理辨识。

## 迭代台账与停止状态

每个场景沿用一个持久台账，最少保存 `run_id / scene_id / scope / max_iterations=null / iterations_used / baseline / best_candidate / stop_reason`。每轮记录 `iteration / hypothesis / input_hashes / changes / tested_range / before_after_metrics / accepted / remaining_issues / evidence_paths`。修正开始验证即计数；环境失败另记，真实覆盖范围不可用计划替代。

执行停止原因使用 `passed / blocked / user_stopped`，与任务、几何、视觉各自验收状态分开。停止时列出实际完成的修正轮数、最佳候选的优点与退化、未解决项及具体停止原因；不能把停止状态写成任务成功。已有完整报告可直接增加这些字段，无需为每轮生成重复长文或渲染全片。

## 首帧静态接受与动态交接

第一步可独立交付 `scene.blend`、相机/尺度与实例清单、机器人初态/手眼来源、首帧各视角 RGB/灰模/五问、静态审计与已知偏差。用户确认后以 `phase_acceptance.json` 绑定候选、scene/config 哈希、原话、接受范围、历史严格证据结果及需重开条件。接受首帧不补成已完成动作或物理。第二步另交付真实事件表、源帧映射、运动/开合参考及来源，随后才是原生控制与 rollout。

## 环节 4 与环节 8 的统计口径

常规五问分母为去重事件代表关键帧数 × 有效视角数；完整五问记录完成、通过和问题关闭分别计数。context 首尾状态、额外完整五问帧、邻域诊断覆盖独立报告。邻域只在问题定位/调整回归时检查受影响项，不计入常规完成率。

第 8 环节按相同源索引输出 real_rgb / blender_rgb / mujoco_rgb，关联同一原生轨迹哈希及两种渲染的相机/投影、分辨率和资产版本。关键帧请求输出三列图像与切换页面；视频请求输出三类视频及对照入口。渲染完成不等于五问通过，三方缺项或材质降级如实标注。

## v3 双输入来源与分阶段状态

robot/human 的输入与重定向记录按 [输入分支契约](input-and-retargeting.md)保存，尤其标明 target_robot、transfer_mode、各臂角色、来源状态/估计、source-to-robot 坐标、时间映射和生成的续接段。分别报告 static_initialization / motion_reference / native_task / appearance / temporal_correspondence / bimanual_coordination；不要用单一 pass 覆盖未测试的能力。无真实对应的续接帧不得加入 real RGB 的共同帧分母。


## 人形候选与接受记录

在现有清单中扩展，不强制新增多个文件：

- `source_station / initial_station / station_delta`：共同坐标系、位置/朝向、来源及不确定性，历史适配站位不能充当原站位。
- `branch=source|A|B|C`：实际站位与接触/路径改动、失败假设、配置/控制哈希、诊断或完整运行范围；未执行分支注明原因。
- `continuity`：阶段衔接、停顿/重试、速度变化、接触/撤离、采样步长、覆盖分母、最长断触与力统计方法。
- `acceptance_basis`：保留接触中断的真实观测、修正与剩余影响，不将其改为零；任务、接触稳定性、几何、姿态与视觉各自结论及选择理由。
- `execution_scope`：固定骨盆/支撑、重力补偿、是否验证自由站立或动态移动。`success_adapted_trajectory` 与精确源轨迹复现分开。
- `native_speed_video / event_aligned_comparison`：请求视频时分别指向实际速度与事件对齐输出，记录源帧到物理时间映射和最终候选哈希。

## 第三步增强记录

按[分层增强](augmentation.md)在现有清单记录：基线和父候选ID、硬件/安装基准、随机种子、选中参数及原值/单位、单项测试范围、原始抽样与实际缩幅系数、支撑派生变化、动作适配、失败原因和验证版本。外观子候选关联父机械配置、资产来源/许可/哈希、显示与碰撞变换、原生结果和渲染状态。

分别统计计划 `(1+N)×(1+M)`、实际生成、机械通过、外观复验通过、媒体完成和有效场景数；筛选后的通过集合不作为随机采样成功率。保存真实测试分母、去重/复用来源、缩幅失败和预算耗尽记录。增强后的 Real RGB 缺失时明确标为原任务参考；不要套用第二步三方同源真值的完成率。

## v5.1 补充

v5.1按[统一依赖清单](metrics-diversity-cache.md#3-缓存依赖清单与恢复协议)关联生产版本、输入依赖、产物哈希和实际验证；物理单位/窗口、去重原因和多样性统计纳入已有清单。

## v5.2 验收入口

按[原生关联与第三步专用验收](native-and-augmentation-gates.md)生成实际证据并运行脚本。仅填写pass或沿用旧完成标记不构成通过。
